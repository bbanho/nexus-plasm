{
  "name": "nexus-plasm",
  "version": "0.1.0",
  "private": true,
  "description": "Clipboard FIFO + LLM processor for Hyprland/Wayland",
  "bin": {
    "plasm": "./bin/plasm.js"
  },
  "scripts": {
    "test": "node --test tests/*.test.js",
    "start": "node bin/plasm.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "yaml": "^2.5.0"
  }
}
